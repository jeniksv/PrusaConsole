// one tab will only complete max common prefix
// two tabs imediatelly after will display possible commands to complete
//
// lets create wrapper for key handling -> something like detect key -> which will return enum { TAB, ARROW KEYs, ENTER, NORMAL_CHAR, CTRL, BACKSPACE}
//
//
// read char should return 
// result should look like console reader with method readline(); readchar();
//
// well a lot of method like leftArrow detected, tab_detected, tabtab_detected
